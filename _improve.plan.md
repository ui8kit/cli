# Баги и ошибки которые нужно исправить

## UI8Kit CLI

- [ ] реализовать установку конфига в корне проекта (./ui8kit.config.json). Если он «ставится» через ui8kit init, разумнее писать его в корень приложения, а не в src/
- [ ] `add button` в CLI не нужно искать среди вариантов, так как они устанавливаются по умолчанию все сразу, поэтому, реализовать поиск в заданной директории без учета регистра. `variants` и `lib` добавить в исключения и скорее всего искать только по `.tsx`
- [ ] проверить зависимости `clsx`, `tailwind-merge` - сейчас они не устанавливаются во время `init`, хотя нужны для утилиты `src/lib/utils.ts` 
- [ ] вариант `src/variants/index.ts` в cdn имеет старую версию и не обновляется во время `init` - необходимо найти причину где это управляется и после сборки я обновлю в CDN